# Backlog

- Add sender-side RTP pacing and congestion-aware bitrate control before another latency claim.
- Measure glass-to-glass end-to-end latency on iPhone Safari with an external high-frame-rate camera.
- Validate rotation and reconnect across several LDPlayer builds.
- Consider audio only after video/control stability is proven.
